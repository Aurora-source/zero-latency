embedding_dim: 896

temporal_transformer:
  num_layers: 16
  num_heads: 8
  embed_dim: 896
  ff_dim: 2048
  dropout: 0.1

social_transformer:
  num_layers: 6
  num_heads: 8
  embed_dim: 896
  ff_dim: 2048
  dropout: 0.1
  distance_sigma: 10.0

scene_context:
  num_layers: 4
  num_heads: 8
  embed_dim: 896
  ff_dim: 2048
  dropout: 0.1
  max_distance: 50.0

goal_prediction:
  num_goals: 6
  hidden_dim: 512
  dropout: 0.1

decoder:
  num_layers: 8
  num_heads: 8
  embed_dim: 896
  ff_dim: 2048
  dropout: 0.1
  future_steps: 12
